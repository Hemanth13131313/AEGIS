apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegis-control-plane.fullname" . }}
  labels:
    {{- include "aegis-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aegis-control-plane.selectorLabels" . | nindent 4 }}
