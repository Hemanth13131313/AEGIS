# AEGIS UI

React/TypeScript UI for the AEGIS SOC dashboard.

## Setup

```bash
npm install
npm run dev
```

## Design System
- Built with standard CSS custom properties mapping to `design.md` tokens.
- Dark-mode first by default.
