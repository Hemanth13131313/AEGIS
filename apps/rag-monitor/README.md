# AEGIS RAG Monitor

Listens for Retrieval events and analyzes them for anomalies (like data exfiltration or embedding manipulations).

## Local Development
```bash
uv run uvicorn app.main:app --reload
```
